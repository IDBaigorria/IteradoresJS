// ═══════════════════════════════════════════════════════════
// PLANTILLA DE COMANDO DINÁMICO (INSTANCIA EXISTENTE)
// ═══════════════════════════════════════════════════════════
// Úsela cuando reciba una instancia que ya cumple la interfaz
// Comando (por ejemplo, generada desde un nodo o recuperada de
// un almacenamiento) y desee registrarla en el sistema.

import { Comando } from '../Comandos/index.js';  // ajuste la ruta
import { Controlador } from '../Controlador/Controlador.js';

/** @type {Comando} */
const comandoExistente = obtenerComandoDeAlgunaFuente(); // ejemplo

if (comandoExistente instanceof Comando) {
    // Opción A: registro inmediato
    Controlador.registrar_comando_desde_instancia(comandoExistente);

    // Opción B: encolar (recomendado si hay posibilidad de que
    // el Controlador aún no esté inicializado)
    // Controlador.encolar_comando(comandoExistente);

    // Ejecutar
    // Controlador.ejecutar_comando(comandoExistente.constructor.nombre(), 'arg1');
} else {
    // Manejar error
    console.error("La instancia no cumple la interfaz Comando.");
}